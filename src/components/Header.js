import logo from '../components/assets/Logo.svg';

function Header() {

    return (
        <header>
            <img src={logo} alt="logo" ></img>
            
        </header>
    )
}

export default Header